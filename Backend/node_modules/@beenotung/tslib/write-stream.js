"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeStream = void 0;
var writeStream;
(function (writeStream) {
    function write(stream, 
    // string or buffer only?
    chunk, encoding) {
        return new Promise((resolve, reject) => {
            if (encoding) {
                const res = stream.write(chunk, encoding, (err) => err ? reject(err) : resolve(res));
            }
            else {
                const res = stream.write(chunk, (err) => err ? reject(err) : resolve(res));
            }
        });
    }
    writeStream.write = write;
})(writeStream || (exports.writeStream = writeStream = {}));
