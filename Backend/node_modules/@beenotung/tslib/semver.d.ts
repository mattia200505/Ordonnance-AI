export declare function to_semver(s: string): number[];
export declare function is_semver(s: string): boolean;
export declare function is_newer(base: number[], compare: number[]): boolean;
export declare function is_compatible(base: number[], compare: number[]): boolean;
export declare enum SemverDiffType {
    breaking = 0,
    compatible = 1,
    same = 2,
    newer = 3
}
export declare function getSemverDiffType(base: number[], compare: number[]): SemverDiffType;
