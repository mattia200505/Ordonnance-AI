export declare namespace search {
    function str_contains(base: string, query: string, caseInsensitive?: boolean): boolean;
    function object_contains_str(base: string | object, query: string | object, caseInsensitive?: boolean): boolean;
    function partialMatch<T>(query: Partial<T>, target: T): boolean;
}
