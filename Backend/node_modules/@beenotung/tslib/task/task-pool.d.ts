import { TaskQueue } from './task-queue';
/** @description concurrent task queue */
export declare class TaskPool extends TaskQueue {
    concurrentSize: number;
    ps: TaskQueue[];
    i: number;
    constructor(concurrentSize?: number);
    runTask<A>(f: () => A | Promise<A>): Promise<A>;
}
