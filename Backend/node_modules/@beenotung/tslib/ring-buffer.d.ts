export declare class RingBuffer<T> {
    readonly size: number;
    private readonly xs;
    private start;
    private end;
    private count;
    constructor(size?: number);
    get length(): number;
    enqueue(x: T): void;
    push(x: T): void;
    dequeue(): T;
    unshift(): T;
}
